# Examples CMakeLists.txt
# This directory contains example projects for UBuilder
# No actual building is done here as examples are built by UBuilder itself

# This file exists to satisfy the parent CMakeLists.txt requirement
# The actual building of examples is done through the build-examples.sh script

message(STATUS "Examples directory configured")
message(STATUS "Run build-examples.sh to build and test example projects")
